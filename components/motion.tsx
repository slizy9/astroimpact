import { motion } from 'framer-motion';
// Export a loosely-typed alias to avoid strict TS issues with intrinsic element typing in this repo.
// This keeps the motion API while avoiding prop-type incompatibilities during migration.
export const M: any = motion;
